# Copy to .env and fill in. .env is gitignored.

NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme
NEO4J_URI=bolt://neo4j:7687

API_PORT=8080
RUST_LOG=debug

# Used by the `ingest` service only — get a free key at https://comicvine.gamespot.com/api/
COMIC_VINE_API_KEY=
