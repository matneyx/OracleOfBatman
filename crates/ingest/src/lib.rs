//! The expanding-crawl algorithm (see /docs/MVP.md ticket 4) lives here,
//! not in main.rs, so the e2e crate can call it directly instead of
//! spawning the CLI as a subprocess. Deliberately empty until ticket 4.
